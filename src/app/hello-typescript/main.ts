var minhaVar= 'minha variavel';

function  minha Func(x,y) {
    return x+y;
}
 //ECMASCRIPT OU ES 2015
let num=2;
const PI=3.14;

//Esse código JSP
var numeros = [1,2,3];
numeros.map(function(valor){
  return valor*2;

});
//É a mesma coisa desse só que ESSE ESTÁ EM ECMASCRIPT 2015
numeros.map(valor => valor*2);

class Matematica {
    soma(x,y){
   return x+y;
    }
}

